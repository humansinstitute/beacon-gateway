# Example environment variables for Beacon Gateway

# Conversion rate for USD to sats: $1 = 1000 sats by default
SATS_PER_DOLLAR=1000

# Example public key for Brain (hex). Replace with your actual value in .env
# BEACON_BRAIN_HEX_PUB=

# CVM server pubkey (hex, 64 chars). Do NOT use npub here.
# BEACON_ID_CVM_PUB=

# Brain CVM private key (hex, 64 chars). Required for CVM client/server comms.
# BRAIN_CVM_PRIVATE_KEY=

# Other commonly used variables (uncomment and set as needed)
# PORT=3000
# SESSION_DIR=.wwebjs_auth
# HEADLESS=true
# NO_SANDBOX=true
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome
# GATEWAY_NPUB=

# LLM configuration
# OPENROUTER_API_KEY=
# INTENT_CONFIDENCE_THRESHOLD=60
