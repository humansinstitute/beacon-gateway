{
  "name": "whatsapp-gateway",
  "version": "0.1.0",
  "description": "Minimal WhatsApp Web JS gateway client using Bun",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.js",
    "start": "bun run src/index.js",
    "start:queue": "bun run src/whatsapp-gateway-queue.ts",
    "start:all": "bun run src/start.ts",
    "dev:all": "bun --watch src/start.ts",
    "start:modular": "bun run src/start.ts",
    "dev:modular": "bun --watch src/start.ts"
  },
  "dependencies": {
    "p-queue": "^8.0.1",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.24.0",
    "puppeteer": "^23.3.0"
  }
}
